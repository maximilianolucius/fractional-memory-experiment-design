\section{Ecological Model and Competing Memory Mechanisms}
\label{sec:model}

\subsection{Strong-Allee predator--prey backbone}
This subsection records the ecological backbone inherited from the previously submitted validated study~\cite{P01}. The model, locked parameterization, coexistence equilibrium, Jacobian, and Matignon stability boundary below are reused inputs rather than new results of the present paper; their derivation and certification belong to Ref.~\cite{P01}. We restate only the formulas needed to define the controlled discrimination experiments.

The nonlinear ecological model is
\begin{align}
 f_1(x,y) &= r x\left(1-\frac{x}{K}\right)\left(\frac{x}{A}-1\right)-\frac{axy}{1+hx},\\
 f_2(x,y) &= e\frac{axy}{1+hx}-my,
\end{align}
with locked parameters
\[
r=\frac32,\qquad K=1,\qquad a=1,\qquad h=\frac12,\qquad e=\frac45,\qquad m=\frac25.
\]
The coexistence equilibrium and Jacobian are
\[
x^*=\frac23,\qquad y^*(A)=\frac{2(2-3A)}{9A},
\]
\[
J(A)=\begin{pmatrix}
\dfrac{7A-2}{8A} & -\dfrac12\\[1ex]
\dfrac{2-3A}{10A} & 0
\end{pmatrix}.
\]
For a commensurate Caputo model, local stability follows the Matignon sector condition~\cite{F05}. In the complex-pair regime $2/7<A<10/19$, the critical order is
\[
\alpha^*(A)=\frac{2}{\pi}\arctan\!\left(\frac{\sqrt{4D(A)-T(A)^2}}{T(A)}\right),
\]
with $T(A)=\operatorname{tr}J$ and $D(A)=\det J$. At $A=0.30$, $\alpha^*\approx0.969012276$, so the fractional system with $\alpha=0.9$ is stable although the integer-order limit is unstable~\cite{P01}. For most discrimination experiments we use $A\le2/7$, where the common operating point remains in the stable regime.


\paragraph{Inherited backbone versus new experimental layer.}
The notation above is intentionally identical to the companion paper because changing the ecological equations would confound the memory-discrimination question with a change of biological mechanism. What changes in the present work is the \emph{response law around the same operating ecology}: the fractional derivative, explicit delay, or finite latent state determines how perturbations propagate through time. This lets all candidates be compared under the same perturbation amplitude, observation horizon, and Allee threshold. Table~\ref{tab:locked-backbone} records the common quantities that are inherited and held fixed unless a numerical sweep explicitly varies $A$ or $\alpha$.

\begin{table}[htbp]
\centering
\caption{Locked ecological backbone used by both manuscripts. These values are inherited inputs, not new contributions of the present paper.}
\label{tab:locked-backbone}
\small
\begin{tabular}{lll}
\hline
\textbf{Quantity} & \textbf{Value} & \textbf{Role in present experiments}\\
\hline
$r$ & $1.5$ & intrinsic prey growth\\
$K$ & $1$ & prey carrying capacity\\
$a$ & $1$ & attack-rate scale\\
$h$ & $0.5$ & Holling-II handling parameter\\
$e$ & $0.8$ & conversion efficiency\\
$m$ & $0.4$ & predator mortality\\
$x^*$ & $2/3$ & common coexistence prey level\\
$y^*(A)$ & $2(2-3A)/(9A)$ & coexistence predator level\\
$J(A)$ & Eq. above & ODE/Caputo local ecological generator\\
\hline
\end{tabular}
\end{table}

The strong-Allee threshold plays two roles in the current manuscript. First, it defines a physically meaningful failure event: if an informative perturbation drives prey below $A$, the experiment may leave the local coexistence regime and trigger extinction dynamics. Second, the distance $x^*-A$ acts as a natural safety margin. Consequently, an input can be statistically attractive while being ecologically unacceptable, which is the central tension explored numerically in Sections~\ref{sec:fd}, \ref{sec:design}, and \ref{sec:benchmark}.

\begin{figure}[htbp]\centering
\includegraphics[width=0.98\textwidth]{fig11_system_schematic}
\caption{Ecological and experimental setting. The strong-Allee backbone and its parameterization are inherited from the previous validated study~\cite{P01}; the intervention/observation architecture and the competing memory laws are specific to the present work. The system is perturbed on the prey channel and observed through prey, predator, or both populations. ODE and Caputo models share the exact ecological Jacobian; DDE and latent models are calibrated rival response laws. The dashed rectangle is a numerical benchmark diagnostic region, not an invariant-set certificate.}
\label{fig:schematic}
\end{figure}

\begin{figure}[htbp]\centering
\includegraphics[width=0.94\textwidth]{fig12_backbone_regime}
\caption{Inherited strong-Allee backbone~\cite{P01} at $A=0.25$, shown here only to define the safety geometry for the new experiment-design problem. Left: representative recovery above the threshold and collapse from below $A$. Right: prey growth term $P(x)$ showing the sign change at the Allee threshold.}
\label{fig:backbone}
\end{figure}

\subsection{Four rival mechanisms}
Dimensional consistency is enforced with the reference time $\tau_0$~\cite{F02,F03,F04}. Around the operating point, the four candidate mechanisms are
\begin{align*}
\text{ODE:}&\quad \dot\xi=J\xi+Bu,\\
\text{Caputo:}&\quad \tau_0^{\alpha-1}{}^CD_t^\alpha\xi=J\xi+Bu,\\
\text{DDE:}&\quad \dot\xi=A_0\xi(t)+A_1\xi(t-\tau)+Bu,\\
\text{latent:}&\quad \dot q=\bar A q+\bar Bu,\qquad y=\bar Cq.
\end{align*}
Only the Caputo mechanism carries a fractional order. The finite latent class is restricted by an explicit state-dimension budget $m$.

\paragraph{Combined fractional-delay stress-test model.}
To address the interaction of memory and explicit delay directly, the numerical study also uses
\begin{align}
\tau_0^{\alpha-1}{}^CD_t^\alpha x(t)
&=P(x(t);A)-\frac{a x(t)y(t)}{1+h x(t)}+u(t),\\
\tau_0^{\alpha-1}{}^CD_t^\alpha y(t)
&=e\frac{a x(t-\tau)y(t)}{1+h x(t-\tau)}-m y(t).
\label{eq:fdmodel}
\end{align}
A constant equilibrium history is used on $[-\tau,0]$. Equation~\eqref{eq:fdmodel} reduces to the pure Caputo model at $\tau=0$ and to the retarded DDE benchmark at $\alpha=1$. It is used as a numerical stress test, not as an additional headline theorem.
