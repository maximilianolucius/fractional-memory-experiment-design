# Manuscript Impact — Round 6

Applied to the chief-repaired (post-R5-audit) manuscript baseline:

- **sec6**: new subsection "A certified full ecological-state theorem via a
  backbone-consistent latent realization" (label `subsec:state_theorem`),
  containing Lemma `lem:polebranch`, Theorem `thm:T23`, Corollary
  `cor:state_testing`, Theorem `thm:T24`, the certified focal tables, and
  Figure `fig:state-theorem` (fig19).  Inserted after the atlas subsection,
  before the section's closing paragraph.
- **main.tex abstract**: factorized-lift obstruction sentence extended with
  the closure via the backbone-consistent realization and headline certified
  numbers.
- **sec1**: the "full ecological-state lift remains open" sentence replaced
  by the closure statement with references to the new results.
- **sec12**: three-levels discussion gains the state-level lift sentence;
  limitations updated so the linearized scope of the new theorem is explicit
  and the nonlinear Volterra extension remains the open item.
- **sec14**: four new index rows (Lemma R6.1, T23, cor:state_testing, T24).
- **figures**: fig19_state_theorem.pdf added (3 panels, interval-certified
  quantities only).  fig18 and the 16 Q1 figures untouched.

Build after integration: expected exit 0, 0 undefined citations/references,
0 warnings (verified in Phase 3; page count grows by the new subsection and
figure).
