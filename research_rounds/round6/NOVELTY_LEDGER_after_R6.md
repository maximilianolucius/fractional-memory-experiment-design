# Novelty Ledger — After Round 6

## Defensible (additions over the post-R5-chief ledger)

1. **Certified full ecological-state finite-latent approximation theorem** for
   the true Caputo predator-prey linearization: the exact response splits into
   a principal-sheet pole pair (matched EXACTLY by an integer-order
   2-dimensional block — the insight that dissolves the Gate-0B backbone
   obstruction) plus a smooth branch density with interval-certified
   finite-mixture quadrature error.  The certified state-level enclosures beat
   the kernel-level ones from m=8 on (0.0274 vs 0.200 at m=16, A=0.25).
2. **Ecological-state testing bound over the same integer-order hierarchy** —
   the discrimination obstruction now holds against genuinely integer-order
   finite-state ecological models at the (linearized) state level, not only at
   the abstract kernel level.
3. **End-to-end interval-certified state-level Strong-Allee pulse-ray budget**
   (closed-form Young/witness sandwich, d_low/d_up >= 0.986) — strictly
   stronger certification status than the kernel-level T22 constants, whose
   maximization remains numerically validated only.
4. The Matignon condition acquires a sharp structural meaning in this problem:
   it is EXACTLY the condition that the principal-sheet pole of the fractional
   backbone is a damped (realizable) integer-order mode.

## Still not defensible

- Any claim for arbitrary safe waveform classes (both T22 and T24 are
  protocol-ray results).
- "First safe model-discrimination theorem" in general; close contemporary
  prior art on safe information limits remains.
- Nonlinear (Volterra) state-level versions — declared future work.
- Any "provably discriminable" label.

## Assessment

The chief's post-R5 assessment was 9.0–9.2 with the explicit remark that "a
clean full ecological-state finite-latent approximation/testing theorem in
Round 6 could justify a higher assessment."  Round 6 delivers exactly that
theorem, with interval certificates stronger than the kernel level, a
structural insight (pole/branch split versus the false factorization) that
explains WHY the previous route failed, and the safety interface in the
preferred outcome A.  Researcher estimate: **9.3–9.5/10**, subject to chief
audit.
