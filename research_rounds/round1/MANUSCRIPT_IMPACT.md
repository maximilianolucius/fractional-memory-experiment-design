# Manuscript Impact — Round 1

## Governance decision

**Do not perform the novelty rewrite of the main manuscript yet.**

Rounds 1–3 remain research/proof rounds. The current manuscript is only patched where a statement or figure is factually misleading.

## Results to hold outside `main.tex` until the Round-3 gate

- new central quantity `Delta_safe^(m)`;
- Round-1 uniform safe-class KL-collapse theorem;
- revised novelty statement relative to arXiv:2607.16895;
- planned Strong-Allee-specific safe energy interface;
- planned complexity-dependent testing bound.

## If R2 and R3 close successfully, expected Round-4 edits

1. **Abstract / Introduction** — replace generic safe-OED novelty language by the three-way interaction:
   fractional approximation + latent complexity + Allee safety.
2. **Section 6** — promote the approximation hierarchy `L_m` and introduce `E_m(alpha,T)` as a first-class quantity.
3. **Section 7/8** — introduce `Delta_safe^(m)` and the Strong-Allee safe budget.
4. **New headline theorem** — combine approximation and safety into an information/testing bound.
5. **Discussion** — position Saligrama 2607.16895 as the generic safety-information antecedent and explain the distinct approximation-class bottleneck.
6. **Figures** — evolve the current Figure 7 / Figure 10 logic into the final Safe Memory-Discrimination Atlas.

## Immediate manuscript patches already justified in this review

These are correctness/visual-governance edits, not a novelty rewrite:

- benchmark rectangle renamed as a diagnostic rectangle unless T17 strict face conditions are actually verified;
- Figure 11 Bayesian loop repaired semantically;
- Figure 7 approximation display and caption repaired;
- "safe-by-construction" removed from amplitude-only linear ranking;
- numerical safe/non-safe trajectories distinguished from invariant-set certificates.
