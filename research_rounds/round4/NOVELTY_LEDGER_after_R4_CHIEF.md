# Novelty Ledger — After Round 4 Chief Audit

Date: 2026-08-14

## What is now defensible

1. The paper has a coherent **kernel-level** complexity-induced testing obstruction: a finite exponential competitor with interval-enclosed L1 error plus a bounded observation/excitation budget yields a quantitative lower bound on composite discrimination error.
2. The all-times state-floor theorem shows why ecological state safety alone cannot replace a physical input/bandwidth restriction.
3. The benchmark exhibits a real safety–informativeness trade-off and a protocol-relative Allee crossover for its locked rivals.

## What is NOT yet defensible as a headline novelty theorem

The paper still does not prove a single theorem in which

`fractional approximation + latent complexity + Strong-Allee common safety`

all enter over the same full rival hierarchy.

The A~0.32 crossover is diagnostic, not hierarchy-uniform.  T20's hierarchy-level numerical example uses the actuator peak cap, not an Allee-derived cap.

Likewise, T20 is currently a kernel-convolution observation theorem, not yet a full ecological state-output approximation theorem for the shared predator-prey backbone.

## Novelty assessment after R4

- concept / paper direction: **about 8.7–9.0 / 10**;
- not yet at the 9.5-level target;
- the likely jump comes from Round 5 if it closes one or both of:
  1. hierarchy-uniform Strong-Allee safe-discrimination regimes;
  2. a non-vacuous ecological-state lift of the kernel approximation/testing bound.

## Claim discipline

Do not claim:

- that QUADPACK error estimates are certificates;
- that the normalized T20 numerical example is the point-sampled benchmark;
- that A~0.32 is a theorem over K_m;
- that the finite m<=32 table proves E_m->0 for the frozen numerical hierarchy;
- that T20 already controls the full nonlinear predator-prey output map.
