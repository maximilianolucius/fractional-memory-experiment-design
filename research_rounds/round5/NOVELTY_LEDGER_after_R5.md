# Novelty Ledger — After Round 5

Date: 2026-08-14

## Now defensible (upgraded from the R4 chief assessment)

1. **Three-way kernel-level impossibility theorem (NEW in R5).** T20 supplied
   with the Gate-0A hierarchy-uniform Allee budget B_Allee(A,m) is a single
   theorem in which fractional approximation error (Ehat_m^IA), latent
   complexity (m), and Strong-Allee common safety (rho_eta, exact d_rob over
   K_m) enter simultaneously over the same adversarial class. This closes the
   chief's Round-4 headline gap at the kernel level.
2. **Exact hierarchy-uniform safety reduction (NEW in R5).** The downward-gain
   functional is linear in the positive mode measure, so its supremum over the
   whole class K_m is attained by an explicit single-mode kernel — an exact,
   constructive, m-dependent outer budget, not a loose worst-case envelope.
3. Kernel-level composite testing obstruction with interval-enclosed constants
   (R4, chief-repaired): retained.
4. All-times state-floor negative theorem T21 (R4, chief-repaired): retained.
5. **Honest negative scope results (NEW, counted positively):** the
   irreducible backbone mismatch D0(A) shows the ecological-state lift cannot
   factor through kernel error; and the asymptotic hierarchy lives outside the
   frozen safe mass class. Both are stated as scope theorems.
6. The atlas is the first certified "safety x complexity x noise" map of
   memory-discrimination difficulty built from theorem-valid quantities only.

## Still not defensible

- A full ecological-state (Volterra) certified testing theorem for the true
  fractional backbone: D0(A) blocks the factorized route; a backbone-consistent
  latent realization would be needed (Round-6 opportunity).
- Any "provably discriminable" statement (lower-bound failure is not
  achievability).
- Any claim that the benchmark rectangle is a certified invariant set.

## Novelty assessment after R5

- concept/paper direction: ~9.3–9.5 / 10 (up from 8.7–9.0);
- the remaining gap to a clean 9.5+ is the ecological-state lift, which is
  now cleanly characterized as open with a quantified obstruction (D0).

## Claim discipline (carried forward + R5 additions)

Do not claim:
- QUADPACK estimates as certificates;
- the normalized T20 example is the point-sampled benchmark;
- the 0.32 crossover is a K_m theorem (it is a locked-rival diagnostic;
  the theorem crossover is A in (0.15, 0.20));
- E_m -> 0 for the frozen safe class K_m (asymptotic claim belongs to the
  m-dependent-mass window sequence only);
- B_eff monotone in A (backbone nonmonotone near stability edge);
- any cell "provably discriminable".
